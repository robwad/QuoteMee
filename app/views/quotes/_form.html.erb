
<%= form_with(model: quote, local: true) do |form| %>
  <% if quote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(quote.errors.count, "error") %> prohibited this quote from being saved:</h2>

      <ul>
      <% quote.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field" align="center">
    <%= form.text_field :author, placeholder: "Author" %>
  </div>
<br>
  <div class="field" align="center">
    <%= form.text_area :text, placeholder: "Description" %>
  </div>
<br>
  <div class="actions" align="center">
    <%= form.submit 'Create Quote', :class => "btn btn-lg btn-success" %> &ensp;
    <a class="btn btn-secondary btn-lg" href="/quotes">Back</a>

  </div>
<% end %>
